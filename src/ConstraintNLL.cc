#include "p2meg/Likelihood.h"

// ============================================================
// 制約項（NLLに加算）
//
// 制約が不要な間は常に 0 を返す。
// 後から制約を入れたくなったら、この関数の中身を編集して足し算する。
// ============================================================

double ConstraintNLL(const std::vector<double>&) {
    return 0.0;
}
